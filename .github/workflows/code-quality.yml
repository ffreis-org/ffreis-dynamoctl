name: Code Quality (SonarCloud)

on:
  workflow_dispatch:
  pull_request:
    types: [opened, synchronize, reopened, ready_for_review]
    paths:
      - '**/*.go'
      - 'go.mod'
      - 'go.sum'
      - 'sonar-project.properties'
      - '.github/workflows/code-quality.yml'
  push:
    branches: [main]

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

permissions: {}

jobs:
  sonar:
    permissions:
      contents: read
    uses: ffreis-org/ffreis-workflows-go/.github/workflows/go-sonar.yml@e181683a6f01ea59aa0de41f8d5ec39e74c5b8ec
    with:
      runner: '["self-hosted","local"]'
      go-version: "1.25"
    secrets:
      SONAR_TOKEN: ${{ secrets.SONAR_TOKEN }}
